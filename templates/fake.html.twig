{% extends "base.html.twig" %}

{% block body %}

    <ul>
        {% for user in users %}
            <li>
                <span style="font-weight: bold">{{ user.name }}</span> - {{ user.email }}
            </li>
        {% endfor %}
    </ul>

{% endblock %}
